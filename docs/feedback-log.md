# 유저 피드백 · 가설 장부

> 유통 과정에서 수집된 피드백과 관측 기록. **여기 적힌 것은 백로그 후보지 약속이 아니다.**
> 반영 여부는 관측 데이터가 정하고, 반영 시점은 관측 종료 후(D5+)다.

## 입력 장벽 (최다 신호 — D5+ 1순위 후보)

| 날짜 | 출처 | 신호 |
|---|---|---|
| 07-15 | 제보 (…fb4b2a) | "기업체의 JD를 복붙하는 형식이 너무 번거로운 것 같애요" |
| 07-17 | 퍼널 실측 | 샘플 체험 7명 → 자기 문서 투입 0명 |
| 07-18 | 오픈카톡 방3 운영진 현직자 A (커뮤니티 운영진) | "공고가 사진인 경우는? 글+사진 섞인 경우도. 공고는 대부분 사진도 있고 표로 된 경우도 많아서 이 처리가 필요할 것 같다" |
| 07-18 | 현직자 A (커뮤니티 운영진) | 이미지 처리에 제미나이(비전) API 제안 |

구현 경로 (설계 확정, 착수는 유통 큐 소진 후):

공고 이미지 — B안: 서버 측 비전 전사 (채택)
- 구조: 이미지 → 비전 모델 전사만("적힌 글자를 그대로 받아 적어라") → 추출 텍스트를 기존 입력창에 삽입 → 유저가 확인·수정 후 기존 제출. 이미지→요구사항 직행 금지 — 측정 안 된 새 파이프라인이 생긴다. 전사만 시켜야 동결 파이프라인이 유지된다
- 진입로 2개: ① 붙여넣기 통합(paste 이벤트에서 클립보드 이미지 감지 → OCR → 커서 위치에 텍스트 삽입. 데스크톱 주력) ② 업로드/드래그 버튼(모바일 폴백 — 모바일은 이미지 클립보드 붙여넣기가 불안정). 두 경로 모두 같은 OCR 엔드포인트, 같은 입력창으로 수렴
- 모델: 기존 스택(OpenAI)의 비전 지원 모델로 통일 — 키·과금·에러 처리 단일 경로. 무료 티어류 학습 이용 이슈 원천 배제
- 안전선: 이미지 디스크 미저장(메모리 전사 후 폐기 — "저장 안 함" 약속의 연장), 파일 크기 상한, OCR 호출 레이트리밋 포함, 비용 실측 후 전역 캡 재계산
- 착수 전 스파이크 (필수 게이트): 실제 이미지 공고 5건을 비전 전사 → 원본과 눈 대조. 전사 품질이 부족하면 착수 자체를 접는다. 판단 비용 ~30분, 몇십 원
- 성공 지표 (사전 정의): 방문→실행 전환율, 기준선 9% (07-18 digest)

공고 이미지 — 임시 다리 (B안 전까지): 입력창 힌트 한 줄 — "이미지로 된 공고는 사진 속 글자를 길게 눌러 복사(아이폰) 또는 구글 렌즈(안드로이드)로 텍스트를 추출해 붙여넣을 수 있어요." 기대치 낮음(힌트는 잘 안 읽힌다), 비용 0이라 넣는 것

공고 이미지 — A안: 브라우저 OCR(Tesseract.js 등): 기각. 한국어 + 디자인 폰트·표 이미지에서 인식 품질 위험 → 깨진 텍스트가 조용히 유입 = 조용한 품질 저하. 이 프로젝트가 금지하는 실패 모드

이력서 쪽 — pdf.js 클라이언트 추출: 기존 계획 유지 (서버 무변경, 추출 결과 유저 확인 후 제출). B안과 같은 라운드에

## 출력 요약 수치 요구

| 날짜 | 출처 | 신호 |
|---|---|---|
| 07-15 | 제보 (…e14aa9, 첫 실유저) | "내 공고가 JD와 몇 %일치하는지 1~100 사이의 숫자로 표시되면 좋을 것 같아요" |

- D1에 자른 "합격 확률 점수"의 수요가 실재함을 확인.
- 자른 이유(근거 없는 숫자)는 유효. 단 **정직한 변형**이 존재: "요구사항 15개 중 근거 발견 10개" — 검증 가능한 셈이라 원칙과 충돌 없음. 이미 계산되는 값. D5+ 결과 화면 노출 검토.

## 케이스 데이터 부족

| 날짜 | 출처 | 신호 |
|---|---|---|
| 07-18 | 현직자 A (커뮤니티 운영진) | "아이템 자체는 괜찮아 보이는데, 케이스 데이터가 많이 필요한 케이스네요" |

- 측정 한계(공고 7건, 판정자 1명)와 일치하는 외부 진단.
- 대응: 유통 = 케이스 수집. 제보 채널이 곧 케이스 데이터 파이프라인.

## 포지셔닝 / 완성도 (현직자 관점)

| 날짜 | 출처 | 신호 |
|---|---|---|
| 07-18 | 현직자 B (현직 개발자) | "취준생을 위한 ~~ 테마가 흔하다. 포폴용이면 테마 피봇 필요, 진짜 제품이면 완성도를 많이 올려야" |
| 07-18 | 현직자 B (현직 개발자) | (긍정) "동작하는 케이스로 결과 봤는데 결과는 생각보다 괜찮네요" |

- 판단: **테마 피봇은 하지 않는다.** 이 프로젝트의 논지는 "테마"가 아니라 "만든 방식(측정·검증·판단의 기록)"이다.
- 진짜 교훈: 현직자가 3분 만에 "흔한 취준생 툴"로 분류했다 = **차별점이 첫인상에서 안 보인다.** README·포폴 소개글에서 과정 스토리를 더 앞세울 것 (이력서 재작성 시 반영).

## 이탈층 피드백 채널 부재 (관측)

| 날짜 | 출처 | 신호 |
|---|---|---|
| 07-17 | 구조 관찰 | 제보 칸이 결과 화면에만 있어 결과까지 안 간 사람(최대 이탈층)은 제보 불가 |

- 랜딩 제보 칸은 반응률 회의적(떠나는 사람은 말없이 떠난다). 커뮤니티 댓글이 실질 대체 채널.

## 유통 채널 기록

| 채널 | 결과 | 교훈 |
|---|---|---|
| 링크드인 (7/15) | 신규 10, 실문서 1, 제보 1 | 예상외 최고 채널. 제품+스토리+직링크 |
| 커리어리 1차 (7/16, 제품 글) | 스팸 분류 → 미노출 | 신규 계정 + 서비스 링크 + 제품 문체 = 분류기 트리거 |
| 커리어리 2차 (7/16, 배움 글 + GitHub 링크) | 노출 418 → 사이트 유입 ≈ 0 | 게이트는 통과, 전환 사망. 노출 ≠ 타겟팅. 우회 링크는 깔때기를 늘린다 |
| 학교 단톡방 (7/16) | 0 | 타깃 아님 (전원 취업) — 예측대로 |
| 오픈카톡 방1 (7/18, 1:1 문의) | 홍보 거절 | 방장이 유사 영역 서비스 운영으로 추정 — 이해충돌 가능성. 운영형 커뮤니티는 이해충돌 → 순수 유저 방 우선 |
| 오픈카톡 방2 (7/18, 1:1 문의) | 검토 대기 | 규칙에 사전 검토 절차 명시된 방 |
| 오픈카톡 방3 (7/18 21:31, 직접 게시) | 하트 2, 현직자 호평, 신규 3+, 실문서 2회, 운영진이 카페 공유 초대 | **최고 성과.** 규칙이 명시 허용하는 방 + 겸손 문구. 규칙이 허용하면 묻지 않는다 — 물으면 허용이 심사로 바뀐다 |

오픈카톡 신규 확장 동결 (07-18 결정) — 멤버 중복으로 도배 인식 위험, 한계효용 급락. 방3은 후속 보고 채널로 유지

## 다음 채널 대기열

- [ ] 방3 연계 네이버 카페 (운영진 초대받음) — **방3 운영진 초대받음.** 커리어리 제품 글 재활용
- [ ] 디스콰이엇 — 프로덕트 승인 대기 중 (7/16 등록)
- [ ] 오픈카톡 방2 — 운영진 검토 대기
